# 测试报告 —— 日期边界与时区专项测试

| 项目 | 内容 |
|---|---|
| 被测系统 | personal-life-os（Django 5.2.17 / Python 3.13） |
| 版本/分支 | `release-v0.9.0` @ `e68f0db` |
| 测试日期 | 2026-08-29 |
| 测试类型 | 功能测试 + 回归测试 + 边界测试 + 静态审查 |
| 测试方法 | 边界值分析、等价类划分、负向测试、一致性测试、配置（时区）测试、全月日历扫描 |
| 执行环境 | Windows 11 / SQLite（测试库）/ `TIME_ZONE=Asia/Shanghai` + `USE_TZ=True` |
| 用例总数 | 26（新增 26，全部自动化） |
| 执行结果 | 26 Pass / 0 Fail（缺陷修复后） |
| 全量回归 | 305 项全过（严格模式 `-W error::RuntimeWarning`，naive datetime 告警 0） |

---

## 1. 测试范围

上一轮修复了「DateTimeField 被 date 过滤导致当月最后一天漏计」的缺陷，该缺陷横跨预算、看板、复盘、首页等多个统计入口。本次针对**同一风险域**做横向展开，避免只验证已修的那一行。

| 模块 | 入口 | 关注点 |
|---|---|---|
| 预算页 | `/budget/` | 月度总额、分类小计、上月同期（跨年） |
| 财务看板 | `/dashboard/` | 选定月份总额、每日趋势、近 6 月趋势区间 |
| 复盘页 | `/review/` | 周区间（周一~周日）、月区间 |
| 首页服务 | `services.home_data()` | 预算卡本月支出 |
| 命令 | `scan_reminders` / `data_check` / `seed_demo` | 同类 naive datetime 写入 |
| 模型 | `ParseJob` | 字段默认值语义 |

---

## 2. 用例设计与执行结果

### 2.1 预算页（TC-BUD）

| 用例编号 | 覆盖点 / 方法 | 关键输入 | 预期结果 | 优先级 | 结果 |
|---|---|---|---|---|---|
| TC-BUD-001 | 边界值-下点 | 100 元 @ 08-01 00:00:00 | 计入本月 | 高 | ✅ |
| TC-BUD-002 | 边界值-上点（**缺陷回归**） | 200 元 @ 08-31 23:59:59 | 计入本月 | 高 | ✅ |
| TC-BUD-003 | 边界值-越界点 | 300 元 @ 09-01 00:00:00 | 不计入 | 高 | ✅ |
| TC-BUD-004 | 边界值-越界点 | 400 元 @ 07-31 23:59:59 | 不计入 | 高 | ✅ |
| TC-BUD-005 | 组合边界 | 上述四点同时存在 | 合计 300 | 高 | ✅ |
| TC-BUD-006 | 等价类-平年 2 月（28 天） | 500 元 @ 2027-02-28 23:59:59 | 计入 | 中 | ✅ |
| TC-BUD-007 | 等价类-闰年 2 月（29 天） | 600 元 @ 2028-02-29 23:59:59 | 计入 | 中 | ✅ |
| TC-BUD-008 | 分类小计（独立查询路径） | 777 元 @ 08-31 20:00 | 分类小计含 777 | 高 | ✅ |
| TC-BUD-009 | 跨年等价类 | 900 元 @ 2026-12-31 23:59:59，today=2027-01-15 | 上月（去年 12 月）合计 900 | 中 | ✅ |

### 2.2 财务看板（TC-DASH）

| 用例编号 | 覆盖点 / 方法 | 关键输入 | 预期结果 | 优先级 | 结果 |
|---|---|---|---|---|---|
| TC-DASH-001 | 选定月份上点 | 150 元 @ 08-31 23:59:59 | `total_expense` = 150 | 高 | ✅ |
| TC-DASH-002 | 等价类-平年 2 月 | 250 元 @ 2027-02-28 23:59:59 | `total_expense` = 250 | 中 | ✅ |
| TC-DASH-003 | 每日趋势末日 | 360 元 @ 08-31 18:00 | `daily[-1]` 为 31 日且金额 360 | 高 | ✅ |
| TC-DASH-004 | 近 6 月区间端点 | 11 元 @ 03-01 00:00、22 元 @ 08-31 23:59:59、99 元 @ 02-28（区间外） | `monthlyExpense` 首 11、末 22，99 被排除 | 高 | ✅ |
| TC-DASH-005 | **全月日历扫描**（日历炸弹回归） | 遍历 08-01 ~ 08-31 逐日请求看板 | 每日均返回 200 | 高 | ✅ |

### 2.3 复盘页（TC-REV）

| 用例编号 | 覆盖点 / 方法 | 关键输入 | 预期结果 | 优先级 | 结果 |
|---|---|---|---|---|---|
| TC-REV-001 | 周区间上点 | 80 元 @ 周日 08-30 23:59:59 | 复盘支出含 80 | 高 | ✅ |
| TC-REV-002 | 周区间越界点 | 90 元 @ 下周一 08-31 00:00:00 | 复盘支出不含 90 | 中 | ✅ |
| TC-REV-003 | 月区间上点 | 120 元 @ 08-31 23:59:59 | 复盘支出含 120 | 高 | ✅ |

### 2.4 首页 / 时区 / 越权 / 月份长度

| 用例编号 | 覆盖点 / 方法 | 关键输入 | 预期结果 | 优先级 | 结果 |
|---|---|---|---|---|---|
| TC-HOME-001 | 首页预算卡 | 480 元 @ 08-31 21:00 | `home_data()["spent"]` = 480 | 高 | ✅ |
| TC-TZ-001 | 一致性测试 | 上月 07-01 00:00 与 07-31 23:59:59 各 50 元 | `__date` 写法与 aware 区间写法结果一致（均 100） | 中 | ✅ |
| TC-TZ-002 | 配置测试-服务端 UTC | 660 元 @ 上海 08-31 20:00（库存 12:00 UTC），override UTC | 仍归属 8 月，计入 | 中 | ✅ |
| TC-TZ-003 | 负向-告警清零 | 连续请求 预算/看板/复盘 | 不产生任何 naive datetime 告警 | 高 | ✅ |
| TC-SEC-001 | 负向-越权隔离 | 他人 9999 元 + 自己 10 元 | 预算页仅计 10 | 高 | ✅ |
| TC-SEC-002 | 负向-越权隔离 | 他人 8888 元 + 自己 20 元 | 看板仅计 20 | 高 | ✅ |
| TC-LEN-001 | 等价类-月份长度 | 28/29/30/31 天月份各记月末 42 元 | 四种月份均计入 | 中 | ✅ |

### 2.5 模型层（TC-JOB）

| 用例编号 | 覆盖点 / 方法 | 关键输入 | 预期结果 | 优先级 | 结果 |
|---|---|---|---|---|---|
| TC-JOB-001 | 默认值语义 | 连续创建 2 条不指定 uuid 的记录 | uuid 互不相同，不报 IntegrityError | 高 | ✅ |
| TC-JOB-002 | 静态断言 | 读取字段 default | 必须是可调用对象，不能是常量 | 中 | ✅ |

---

## 3. 缺陷清单

| 缺陷编号 | 严重级 | 模块 | 现象 | 根因 | 影响面 | 状态 |
|---|---|---|---|---|---|---|
| BUG-01 | **P1 高** | 财务看板 `dashboard` | 页面 500 | `three_avg`（float）× `CATEGORY_SPIKE_RATIO`（Decimal）抛 `TypeError` | 建议仅在 `day % 3 == 0` 生成 → **每月约 10 天**，任何有支出数据的用户打开看板即 500（实测 31 天中 10 天失败） | ✅ 已修 |
| BUG-02 | P2 中 | 模型 `ParseJob.uuid` | 第二条记录 `IntegrityError` | `default=uuid.uuid4().hex` 在类定义时**立即求值**，默认值固化为常量，与 `unique=True` 冲突 | 任何不显式传 uuid 的创建路径第 2 条即失败；同时每次 `makemigrations` 都产生多余迁移（持续迁移漂移） | ✅ 已修 |
| BUG-03 | P3 低 | 服务 `home_data` | `AttributeError: 'str' object has no attribute 'strftime'` | `TimeField(default="10:00")` 是字符串默认值，Django 仅在从 DB 读取时转成 `time`；内存新建实例仍是 `str` | 仅影响「创建用户后对同一内存实例调用 `home_data()`」的场景（测试、脚本、未来欢迎页）。注册走 redirect 属新请求，生产当前不受影响 | ✅ 已修 |
| BUG-04 | P2 中 | 工程规范 | `makemigrations --check` 失败 | 模型与迁移不同步（BUG-02 的连带结果） | CI 若加迁移检查会失败；易被误生成含随机默认值的迁移 | ✅ 已修（补 `0028`） |

### 缺陷发现过程说明

- **BUG-01** 属**日历相关缺陷**：触发与否取决于运行日期。原有 228 项测试全绿，仅仅是因为「今天恰好是 29 号」（29 % 3 ≠ 0）。这类缺陷必须靠**按天扫描**而非只测当天，故补了 TC-DASH-005。
- **BUG-02/03** 属**默认值语义缺陷**：均因「把函数调用写成函数调用的结果」。
- 三条回归测试（TC-BUD-002、TC-DASH-005、TC-JOB-001/002）均已**回退修复后实测失败**，确认非空测试。

---

## 4. 修复与验证

| 项 | 处理 |
|---|---|
| BUG-01 | `cat_month > three_avg * float(CATEGORY_SPIKE_RATIO)`；并全仓扫描其余 `float` 用法，确认仅此一处混算（其余均为「Decimal 运算后再转 float」） |
| BUG-02 | 新增模块级 `_default_parse_job_uuid()` 可调用工厂作为 default；生成迁移 `life/migrations/0028_alter_parsejob_uuid.py` |
| BUG-03 | 改为同时兼容 `time`（`strftime`）与 `str`（截断为 `HH:MM`） |
| 迁移同步 | `makemigrations --check` 现为 `No changes detected` |

### 回归验证

| 项目 | 结果 |
|---|---|
| 专项用例 | 26 / 26 通过 |
| 全量测试（严格模式） | **305 / 305 通过**，`-W error::RuntimeWarning` 下无任何 naive datetime 告警 |
| `ruff check life/ accounts/ common/` | All checks passed |
| `manage.py check` | 0 issues |

---

## 5. 结论与建议

1. **结论**：专项测试的 26 条用例全部通过，期间发现并修复 4 项缺陷（1 项 P1、2 项 P2、1 项 P3）。日期边界域的修复经边界值/等价类验证有效，未发现新的漏计问题。
2. **建议一（高）**：CI 增加 `makemigrations --check --dry-run` 步骤。本次 BUG-04 若非专项测试触发，会一直潜伏到上线。
3. **建议二（中）**：对「按日期/周期触发」的逻辑统一补**日历扫描**用例（如 TC-DASH-005 模式），避免缺陷随日期漂移而漏网。
4. **建议三（中）**：`constants.py` 中比率常量混用 `Decimal` 与 `int`（如 `CATEGORY_SPIKE_RATIO` 为 Decimal、`CATEGORY_GROWTH_FACTOR` 为 int）。建议统一为 `float`（用于比值比较）或统一 `Decimal`（用于金额运算），从类型层面消除 BUG-01 这类混算隐患。
5. **遗留**：BUG-03 的根因（`TimeField` 使用字符串默认值）本次以保证健壮性的方式规避，未改动模型默认值（避免额外迁移）。如需根治，可将默认值改为 `datetime.time(10, 0)` 并生成迁移。
