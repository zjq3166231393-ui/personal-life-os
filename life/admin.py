# Life app admin registrations.
#
# Historical note: the legacy ``Entry`` model (a redundant dual-write copy of
# Expense/Task/Note) was removed in v0.9.1, and its admin registration went with
# it. Core models are intentionally kept out of the Django admin to keep the
# single-user personal-data surface minimal.

