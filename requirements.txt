Django>=5.0,<6.0
PyMySQL>=1.1
python-dotenv>=1.0
cryptography>=42.0
openai>=1.0
zhdate>=0.1
redis>=5.0
# 生产 WSGI 服务器（Docker / PaaS 启动依赖，缺失会导致容器起不来）
gunicorn>=21.2
# 生产环境由 Django 直接托管静态文件（DEBUG=False 后不再服务 CSS/JS）
whitenoise>=6.6

